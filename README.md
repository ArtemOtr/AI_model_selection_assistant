# AI Model Selection Assistant

[![Python](https://img.shields.io/badge/Python-3.13%2B-blue)](https://python.org)


Интеллектуальный ассистент для клиентов MWS, который помогает
выбрать подходящую GPT Model Hub‑модель под их продуктовый кейс и
бюджет. 


---

##  Для чего это нужно?

При выборе LLM для production-задач разработчики сталкиваются с десятками моделей от разных провайдеров. Каждая модель имеет десятки характеристик: размер, контекстное окно, цена за токен, скорость инференса, качество на разных доменах.

Ассистент автоматизирует подбор: принимает требования на естественном языке, фильтрует модели по техническим ограничениям, ранжирует по приоритетам пользователя и выдаёт топ-K рекомендаций с объяснением.

---

## 🧠 Архитектура и роли агентов

```
Пользователь → [OpenAI API] → Runner ADK → mws_orchestrator (LLM_Agent)
                                                 │
                                     ┌───────────┼───────────┐
                                     ▼           ▼           ▼
                            get_snapshot  filter_models  score_models
                                     │           │           │
                                     └───────────┼───────────┘
                                                 ▼
                                          ParserCacheAgent
                                                 │
                                                 ▼
                                          MWSTableScraper → mws.ru
```

### Компоненты

**mws_orchestrator** — основной агент на Google ADK (LlmAgent). Принимает запрос пользователя на естественном языке, извлекает структурированные требования, вызывает инструменты фильтрации и ранжирования, формирует финальный ответ.

**SelectionTools** — набор инструментов агента:

- `get_models_snapshot()` - возвращает актуальный список всех моделей из кэша.
- `filter_models_by_requirements` — отсеивает модели по контексту, размеру и бюджету
- `score_models` — ранжирует оставшиеся по формуле, учитывающей приоритеты пользователя (качество, цена, скорость)

**ParserCacheAgent** — кэширующий парсер. Раз в N секунд (по умолчанию 300) обновляет данные о моделях с mws.ru, хранит в памяти в виде pandas DataFrame и списка словарей. Работает асинхронно, не блокирует запросы.

**MWSTableScraper** — низкоуровневый парсер HTML-таблиц. Извлекает характеристики моделей и цены с двух страниц mws.ru, склеивает в единую таблицу.

### Pipeline обработки запроса

1. Пользователь отправляет запрос → агент извлекает из запроса пользователя основные пожелания в словарь `requirements`
2. `filter_models_by_requirements(requirements)` → отсев по контексту, размеру, бюджету
3. `score_models(result)` → ранжирование отфильтрованных моделей
4. Агент возвращает топ-K моделей с объяснением

---

## 📡 OpenAI-совместимый API

Сервер реализован на asyncio без фреймворков, полностью совместим с OpenAI API. 

### Эндпоинты

| Метод | Путь | Описание                        |
|-------|------|---------------------------------|
| GET | `/v1/models` | Список доступных моделей агента |
| POST | `/v1/chat/completions` | Чат-взаимодействие с агентом    |

### Пример запроса и ответа

Благодаря хранению сессий в `InMemorySessionService` пользователь имеет возможность вести диалог с ассистентом, 
чтобы делать различные уточнения. Для это необходимо в следующие запросы `session_id` со значением из ответа модели
добавлять ключ 

**1-ый запрос:**

```
    POST http://localhost:8000/v1/chat/completions
        -H "Content-Type: application/json
        -d '{
            "model": "mistral-large",
            "messages": [
              {"role": "user", "content": "Нужна модель для чат-бота поддержки. Бюджет до 5000 руб/мес, 100000 запросов, вход 500 токенов, выход 300. Контекст от 16K. Топ-3."}
            ]
          }'
```

**Ответ (200):**

```json
{
  "id": "chatcmpl-1777224961063",
  "object": "chat.completion",
  "created": 1777224961,
  "model": "mistral-large",
  "choices": [
    {
      "index": 0,
      "message": {
        "role": "assistant",
        "content": "На основе ваших требований для чат-бота поддержки с бюджетом до **5000 руб/мес**, **100 000 запросов**, средней длиной входа **500 токенов** и выхода **300 токенов**, а также необходимостью контекста **от 16K токенов**, я подобрал топ-3 модели:\n\n---\n\n### **1. Kimi K2 Instruct (Moonshot AI)**\n- **Размер модели:** 1024B параметров\n- **Контекст:** 128K токенов\n- **Оценка:** 9.58 (лучший результат)\n- **Почему подходит:**\n  - Огромный контекст (128K) позволяет хранить длинную историю диалогов, что критично для поддержки.\n  - Высочайшее качество ответов благодаря большому размеру модели.\n  - Оптимизирована для диалоговых систем и работы с большими объёмами текста.\n  - Несмотря на высокую стоимость, укладывается в бюджет при заданных объёмах запросов.\n\n---\n\n### **2. Qwen3 Coder 480B A35B (QWEN)**\n- **Размер модели:** 480B параметров\n- **Контекст:** 256K токенов\n- **Оценка:** 5.52\n- **Почему подходит:**\n  - Один из крупнейших контекстов (256K) среди доступных моделей.\n  - Отлично справляется с задачами, требующими глубокого понимания контекста.\n  - Экономичнее Kimi K2, но при этом сохраняет высокое качество.\n  - Подходит для сложных сценариев поддержки, где требуется анализ больших объёмов данных.\n\n---\n\n### **3. GLM-4.6 357B (Z.ai)**\n- **Размер модели:** 357B параметров\n- **Контекст:** 200K токенов\n- **Оценка:** 4.20\n- **Почему подходит:**\n  - Большой контекст (200K) и высокая производительность.\n  - Оптимизирована для работы с несколькими языками, что может быть полезно для мультиязычной поддержки.\n  - Более доступная по цене, чем Kimi K2, но при этом сохраняет высокое качество.\n  - Хороший баланс между стоимостью и качеством для задач поддержки.\n\n---\n\n### **Рекомендации:**\n- Если **качество** — ваш главный приоритет, выбирайте **Kimi K2 Instruct**. Это лучшая модель в подборке, которая обеспечит максимально точные и контекстно-осознанные ответы.\n- Если вам нужен **баланс между качеством и стоимостью**, обратите внимание на **Qwen3 Coder 480B** или **GLM-4.6 357B**. Обе модели предлагают отличный контекст и высокую производительность при более низкой стоимости.\n- Все три модели укладываются в ваш бюджет и соответствуют требованиям по контексту и объёмам запросов."                      
      },
      "finish_reason": "stop"
    }
  ],
  "usage": {
    "prompt_tokens": 0,
    "completion_tokens": 0,
    "total_tokens": 0
  },
  "session_id": "3075e5ad-fef4-41f4-9cec-0994db889b98"
}
```
**2-ой запрос в рамках той же сессии:**
```
   POST http://localhost:8000/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{
    "model": "mistral-large",
    "session_id": "3075e5ad-fef4-41f4-9cec-0994db889b98",
    "messages": [
      {"role": "user", "content": "А если бюджет сократить до 3000 рублей?"}
    ]
  }'
```
**Ответ (200):**
```json
{
  "id": "chatcmpl-1777225092131",
  "object": "chat.completion",
  "created": 1777225092,
  "model": "mistral-large",
  "choices": [
    {
      "index": 0,
      "message": {
        "role": "assistant",
        "content": "При бюджете **3000 руб/мес** и тех же требованиях (100 000 запросов, контекст от 16K, качество в приоритете) топ-3 модели остаются теми же, но их **стоимость может превышать ваш бюджет**. Вот что стоит учесть:\n\n---\n\n### **1. Kimi K2 Instruct (Moonshot AI)**\n- **Размер:** 1024B параметров\n- **Контекст:** 128K токенов\n- **Оценка:** 9.58 (лучшая модель)\n- **Проблема:** Скорее всего, **превысит бюджет 3000 руб/мес** при 100 000 запросах.\n- **Рекомендация:** Если качество критично, попробуйте сократить количество запросов или увеличить бюджет.\n\n---\n\n### **2. Qwen3 Coder 480B A35B (QWEN)**\n- **Размер:** 480B параметров\n- **Контекст:** 256K токенов\n- **Оценка:** 5.52\n- **Проблема:** Также может **выйти за рамки бюджета**, но дешевле Kimi K2.\n- **Рекомендация:** Подходит, если нужно **максимальное качество в рамках 3000 руб/мес**, но с риском превышения бюджета.\n\n---\n\n### **3. GLM-4.6 357B (Z.ai)**\n- **Размер:** 357B параметров\n- **Контекст:** 200K токенов\n- **Оценка:** 4.20\n- **Проблема:** Более экономичная, но всё равно **может не уложиться в 3000 руб/мес**.\n- **Рекомендация:** Лучший вариант из топ-3 для **строгого бюджета**, если готовы немного пожертвовать качеством.\n\n---\n\n### **Альтернативы в рамках бюджета:**\nЕсли ни одна из топ-3 моделей не укладывается в 3000 руб/мес, рассмотрите **более компактные модели** из списка:\n\n1. **Llama-3.3-70B-Instruct (Meta)**\n   - **Размер:** 70B параметров\n   - **Контекст:** 128K токенов\n   - **Оценка:** 1.40\n   - **Почему:** Надёжная модель с хорошим балансом цены и качества. Скорее всего, **уложится в бюджет**.\n\n2. **DeepSeek-R1-Distill-Qwen-32B (DeepSeek)**\n   - **Размер:** 32B параметров\n   - **Контекст:** 128K токенов\n   - **Оценка:** 1.08\n   - **Почему:** Экономичная модель с большим контекстом. Подходит для **ограниченного бюджета**.\n\n3. **Gemma-3-27B-IT (Google)**\n   - **Размер:** 27B параметров\n   - **Контекст:** 128K токенов\n   - **Оценка:** 1.03\n   - **Почему:** Лёгкая и быстрая модель, но с меньшим качеством.\n\n---\n\n### **Вывод:**\n- **Если бюджет строгий (3000 руб/мес)**, выбирайте **Llama-3.3-70B-Instruct** или **DeepSeek-R1-Distill-Qwen-32B**.\n- **Если можно немного увеличить бюджет**, рассмотрите **GLM-4.6-357B** или **Qwen3 Coder 480B**.\n- **Kimi K2 Instruct** — лучшая по качеству, но **гарантированно превысит бюджет**."                                                                                                                            
      },
      "finish_reason": "stop"
    }
  ],
  "usage": {
    "prompt_tokens": 0,
    "completion_tokens": 0,
    "total_tokens": 0
  },
  "session_id": "3075e5ad-fef4-41f4-9cec-0994db889b98"
}
```


**GET /v1/models:**

```json
{"object": "list",
  "data":
  [
    {"id": "llama-3.3-70b", "object": "model", "created": 1777039497, "owned_by": "huggingface"},
    {"id": "qwen-72b", "object": "model", "created": 1777039497, "owned_by": "huggingface"}, 
    {"id": "groq-llama", "object": "model", "created": 1777039497, "owned_by": "groq"}, 
    {"id": "gemini-flash", "object": "model", "created": 1777039497, "owned_by": "gemini"}
  ]
}
```

---

## 🚀 Локальный запуск

### Вариант 1: Развертывание локального API

```bash
# Клонирование
git clone https://github.com/ArtemOtr/AI_model_selection_assistant.git
cd AI_model_selection_assistant

# Виртуальное окружение
python -m venv .venv
source .venv/bin/activate  # Linux/Mac
# .venv\Scripts\activate   # Windows

# Зависимости
pip install -r requirements.txt

# Конфигурация
cd agents
cp .env.example .env
# Отредактируйте .env — добавьте нужные вам ключи
# Отредактируйте agents/agents_config.json.
# - Добавьте нужные вам модели
# - Добавьте переменные окружения в поля api_key_env нужных моделей.
# - Выберите модель по умолчанию (default)
cd ..
# Запуск OpenAI-совместимого API
python app.py
```

Сервер запустится на `http://0.0.0.0:8000`.

### Вариант 2: ADK Web (локальный веб-интерфейс для работы с агентом Google ADK)

```bash
git clone https://github.com/ArtemOtr/AI_model_selection_assistant.git

cd AI_model_selection_assistant

adk web
```

Откройте `http://127.0.0.1:8000`, выберите app `agents`.


### Конфигурация моделей (agents/agents_config.json)

Файл содержит пример конфигурации, на основе которой при локальном запуске можно настроить свою конфигурацию под себя.
Модели не от Google передаются в ADK агента с помощью LiteLlm. **Обратите внимание, что не все сторонние модели
смогут использовать правильно инструменты ADK (иметь возможность делать tool-calling)**

```json
{
  "default": "llama-3.3-70b",
  "models": {
    "llama-3.3-70b": {
      "provider": "huggingface",
      "model": "huggingface/together/meta-llama/Llama-3.3-70B-Instruct",
      "api_key_env": "HF_TOKEN"
    }
  }
}
```

- `default` — модель, используемая если в запросе не указана конкретная
- `provider` — `gemini` для Google моделей (возвращается строка), любой другой для LiteLlm
- `api_key_env` — имя переменной из `.env`, где лежит API-ключ

---

## 📁 Структура проекта

```
AI_model_selection_assistant/
├── agents/
│   ├── agent.py              # Точка входа: загрузка конфига, создание агента
│   ├── selection_agent.py    # Агент подбора: инструкция, фильтрация, скоринг
│   ├── parser_agent.py       # Кэширующий парсер таблиц с mws.ru
│   ├── mws_scraper.py        # Низкоуровневый HTML-парсер
│   ├── .env.example          # Пример переменных окружения
│   └── agents_config.json   # Конфигурация доступных LLM-моделей
├── app.py                    # OpenAI-совместимый HTTP-сервер на asyncio
├── requirements.txt          # Python-зависимости
└── README.md                 # Этот файл
```

---

